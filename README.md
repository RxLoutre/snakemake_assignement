# snakemake_assignement
First snakemake pipeline since 2016 🦦

Goal : Perform alignment of paired end reads and coverage statistics on the alignment.

#Installation

After cloning this repository, you can create a conda environment were all proper tools are installed for this pipeline to function.

```bash
conda env create -f nice_otter.yml
conda activate nice_otter.yml
```

The file config.json should be edited and adjusted to your own paths and samples.
